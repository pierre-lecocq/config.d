# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

set $mod Mod4
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
#font -misc-fixed-medium-r-normal--13-120-75-75-c-80-iso10646-1
font -misc-fixed-medium-r-normal--15-140-75-75-c-90-iso10646-1
#font pango:DejaVu Sans Mono 10
floating_modifier $mod

# gaps
gaps inner 10
gaps outer 10

# Remove title bar
new_window pixel 2

# Apps
bindsym $mod+Return exec rxvt-unicode #bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+d exec dmenu_run -nb '#313131' -nf '#cccccc' -p run
bindsym $mod+l exec i3lock -c 414141

# Actions
bindsym $mod+Shift+Q kill

bindsym $mod+Shift+C reload
bindsym $mod+Shift+R restart
bindsym $mod+Shift+E exit

# Moves
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split
bindsym $mod+h split horizontal
bindsym $mod+v split vertical

# Fullscreen
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+z layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+space focus mode_toggle
bindsym $mod+Shift+space floating toggle

# Focus
bindsym $mod+a focus parent
#bindcode $mod+d focus child

# Workspace
bindsym $mod+ampersand workspace 1
bindsym $mod+eacute workspace 2
bindsym $mod+quotedbl workspace 3
bindsym $mod+apostrophe workspace 4
bindsym $mod+parenleft workspace 5
bindsym $mod+minus workspace 6

bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6

# Resize
mode "resize" {
    bindsym Right resize shrink width 10 px or 10 ppt
    bindsym Up resize grow height 10 px or 10 ppt
    bindsym Down resize shrink height 10 px or 10 ppt
    bindsym Left resize grow width 10 px or 10 ppt
    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Multimedia keys
bindsym XF86AudioRaiseVolume exec "amixer -c 0 set Master 5%+ unmute"
bindsym XF86AudioLowerVolume exec "amixer -c 0 set Master 5%- unmute"
bindsym $mod+Shift+P exec "scrot"

# i3 bar
bar {
    position top
    status_command i3status
    colors {
           background #313131
           focused_workspace  #ffffff #1D68C4
           active_workspace   #ffffff #313131
           inactive_workspace #888888 #313131
    }
}

# Colors [class border backgr. text indicator]
client.focused #414141 #1D68C4 #ffffff #cccccc
client.focused_inactive #414141 #414141 #cccccc #cccccc
client.unfocused #414141 #414141 #cccccc #cccccc

# Exec at startup
# exec xsetroot -solid '#414141'
exec --no-startup-id xset b 0 0
exec --no-startup-id numlockx on
exec --no-startup-id synclient tapButton1=1
exec --no-startup-id dunst
exec --no-startup-id feh --bg-scale ~/images/wallpapers/frenchtouch.jpg
# exec --no-startup-id xrandr --output HDMI1 --off --output LVDS1 --mode 1920x1080 --pos 0x0 --rotate normal --output DP1 --off --output VGA1 --mode 1680x1050 --pos 1920x0 --rotate normal
