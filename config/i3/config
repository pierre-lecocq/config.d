# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

set $mod Mod4
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
#font -misc-fixed-medium-r-normal--13-120-75-75-c-80-iso10646-1
#font -misc-fixed-medium-r-normal--15-140-75-75-c-90-iso10646-1
#font pango:DejaVu Sans Mono 10
font pango:Source Sans Pro, IconicIcons 10

floating_modifier $mod

# Colors (1D68C4 = blue, D64937 = red)

set $maincolor #1D68C4
# set $maincolor #D64937

# Gaps : https://github.com/Airblader/i3/wiki/Compiling-&-Installing
gaps inner 10
gaps outer 5

# Remove title bar
new_window 1pixel

# Apps
bindsym $mod+Return exec rxvt-unicode
bindsym $mod+d exec dmenu_run -nb '#313131' -nf '#cccccc' -p run
bindsym $mod+l exec --no-startup-id "i3lock -c 121212; systemctl suspend;"

# Actions
bindsym $mod+Shift+Q kill

bindsym $mod+Shift+C reload
bindsym $mod+Shift+R restart
bindsym $mod+Shift+E exit

# Moves
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split
bindsym $mod+h split horizontal
bindsym $mod+v split vertical

# Fullscreen
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+z layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+space focus mode_toggle
bindsym $mod+Shift+space floating toggle

# Focus
bindsym $mod+a focus parent
#bindcode $mod+d focus child

# Workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6

bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6

# Resize
mode "resize" {
    bindsym Right resize shrink width 10 px or 10 ppt
    bindsym Up resize grow height 10 px or 10 ppt
    bindsym Down resize shrink height 10 px or 10 ppt
    bindsym Left resize grow width 10 px or 10 ppt
    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Multimedia keys
bindsym XF86AudioRaiseVolume exec "~/bin/volume inc"
bindsym XF86AudioLowerVolume exec "~/bin/volume dec"
# bindsym XF86AudioMute exec "~/bin/volume mute"
# bindsym XF86AudioPlay exec ""
# bindsym XF86AudioNext exec ""
# bindsym XF86AudioPrev exec ""

# Screenshot fullscreen
bindsym Print exec "scrot -q 100 ~/screenshot-%Y%m%d-%H%M%S.png"
# Screenshot current focused window
bindsym Shift+Print exec "scrot -q 100 -u ~/screenshot-%Y%m%d-%H%M%S.png"

# i3 bar
bar {
    position top
    # status_command i3status --config ~/.config/i3/status.config
    status_command py3status -c ~/.config/i3/py3status.config

    colors {
           background #212121
           focused_workspace  #ffffff $maincolor
           active_workspace   #ffffff #313131
           inactive_workspace #888888 #212121
    }
}

# Colors [class border backgr. text indicator]
client.focused #414141 $maincolor #ffffff #cccccc
client.focused_inactive #414141 #414141 #cccccc #cccccc
client.unfocused #414141 #414141 #cccccc #cccccc

# Exec at startup
#exec --no-startup-id compton
exec --no-startup-id xset b 0 0
exec --no-startup-id numlockx on
exec --no-startup-id synclient tapButton1=1
exec --no-startup-id dunst
exec --no-startup-id feh --bg-scale ~/images/wallpapers/wallpaper.jpg
exec --no-startup-id setxkbmap -option grp:alt_shift_toggle gb,fr
# exec --no-startup-id xsetroot -solid '#121212'
# exec --no-startup-id conky -c ~/.config/conky/conkyrc
